"""
Permian Well Economics Engine — Core Analytics Package

Modules:
    decline_curves  — Arps DCA fitting, EUR calculation, production forecast
    well_economics  — Cash flow model, PV10, IRR, breakeven, sensitivity
    type_curves     — Basin type curve aggregation and operator analysis
    data_loader     — EIA/RRC data ingestion and sample data generation
    visualization   — Chart templates, colors, CSS (import from here)
    export_utils    — PNG, Excel, shareable link generation
    session_state   — Streamlit session state management
"""
